#GitHub cheat sheet

### initial repo setup
Create a github repo and delete readme files, etc.

git init
git config --global user.name 'username'
git config --global user.email 'email'
git remote add origin https://github.com/patricktouchette/mysql-course.git
git pull origin master

git add .
git commit -m "First Commit"
git push origin master

### TO commit changes

git status
git add --all
git commit -m "update"
git push origin master


### To change origin
git remote set-url origin https://github.com/patricktouchette/mysql-course.git



for reference, check this out
https://community.c9.io/t/instructions-to-port-push-cloud9-workspace-to-github/16830/19